<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.big.data.mapper.system.SystemUserMapper">
    <select id="query" resultType="com.big.data.bean.system.SystemUser">
        select
            id,
            username username,
            password password
         from t_system_user where username = #{username,jdbcType=VARCHAR} and password = #{password,jdbcType=VARCHAR}
    </select>

</mapper>
